# Example .env — every rule tripped at least once.
JWT_ALGORITHM=none                             # JWT-A001
JWT_SECRET=                                    # JWT-A003
NEXTAUTH_SECRET=changeme                       # JWT-A004
SESSION_SECRET=<REPLACE_ME>                    # JWT-A005
AUTH_SECRET=aaaaaaaaaaaaaaaa                   # JWT-A006 (low entropy)
