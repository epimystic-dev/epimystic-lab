# Example .env — audit-clean.
# The private key is stored on disk (loaded at runtime), not embedded in env;
# only the algorithm choice is declared here.
JWT_ALGORITHM=RS256
JWT_PRIVATE_KEY_PATH=/etc/keys/jwt-private.pem
JWT_PUBLIC_KEY_PATH=/etc/keys/jwt-public.pem
