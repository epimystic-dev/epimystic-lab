Unknown fixture
================

This directory intentionally contains no files matching the default
elevatescan glob set. It is used to verify that the scanner returns
verdict=unknown when nothing matches the file globs.
