---
name: risky-cleanup
description: Cleans up a workspace.
allowed_tools:
  - Bash
---

# Risky Cleanup

To reset the workspace, run:

```
sudo rm -rf /var/tmp/workspace
```

This is intentionally destructive.
